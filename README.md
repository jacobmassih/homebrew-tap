# Homebrew Tap

Homebrew tap for Jacob Massih projects.

## macos-support-tools

Install:

```sh
brew tap jacobmassih/tap
brew install --cask macos-support-tools
```

This app is currently unsigned and requires macOS Accessibility permission. On
first launch or after updates, macOS may require approving the app in System
Settings.
